# opencode environment setup — source this file in ~/.bash_aliases or /etc/profile.d/opencode.sh
# Resolve the current WireGuard IP from secrets.local.md if needed.
export OPENCODE_MODELS_URL=http://10.8.0.18:11436
